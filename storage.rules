rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default: deny all access (will be configured in Task 4.6)
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
