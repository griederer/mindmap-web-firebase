rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all access (will be configured in Task 4.1)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
